# ╔══════════════════════════════════════════════════╗
# ║     Музыканты Backend — Environment Variables    ║
# ╠══════════════════════════════════════════════════╣
# ║  Скопируйте в .env и заполните значения          ║
# ║  НЕ коммитьте .env в git!                        ║
# ╚══════════════════════════════════════════════════╝

# ─── Server ───────────────────────────────────────
NODE_ENV=development
PORT=3000
# Уровень логов: error, warn, info, debug
# LOG_LEVEL=info

# ─── Database (PostgreSQL) ────────────────────────
# Формат: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://musicians:password@localhost:5432/musicians_db

# ─── Redis ───────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
# В production: сгенерируйте пароль: openssl rand -hex 16

# ─── JWT Authentication ───────────────────────────
# ОБЯЗАТЕЛЬНО: минимум 32 символа!
# Сгенерировать: openssl rand -hex 32
JWT_SECRET=your-secret-key-change-me-minimum-32-chars
JWT_EXPIRES_IN=7d

# ─── Admin Login ──────────────────────────────────
# ОБЯЗАТЕЛЬНО для входа в админ-панель.
# Сгенерировать хеш: npm run generate-password-hash (или node scripts/generatePasswordHash.js)
# Затем ввести пароль и вставить полученный хеш сюда
ADMIN_PASSWORD_HASH=

# ─── Telegram Bots ─────────────────────────────────
# Получить у @BotFather в Telegram
TELEGRAM_ADMIN_BOT_TOKEN=123456789:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# Опционально: отдельный бот для пользователей (Mini App, deep links)
# TELEGRAM_USER_BOT_TOKEN=987654321:XYZ-abc1234...
# Username пользовательского бота без @ (для ссылок вида t.me/username)
# TELEGRAM_USER_BOT_USERNAME=your_user_bot
# Имя Mini App из BotFather /newapp (для direct links с параметрами: t.me/username/appname?startapp=...)
# ВАЖНО для голосования! Без этого параметры не передаются в Mini App.
# TELEGRAM_MINI_APP_NAME=app

# ─── URLs ─────────────────────────────────────────
# URL фронтенда для CORS и ботов. ОБЯЗАТЕЛЬНО в production.
# Несколько источников через запятую: https://app.example.com,https://admin.example.com
# Development: http://localhost:5173
# Production: https://your-domain.com
FRONTEND_URL=http://localhost:5173

# Опционально: админ-панель (по умолчанию: FRONTEND_URL/admin)
# ADMIN_PANEL_URL=http://localhost:5173/admin

# Опционально: URL Mini App для кнопок в User Bot (по умолчанию: FRONTEND_URL)
# MINI_APP_URL=http://localhost:5173

# ─── File Upload ──────────────────────────────────
# Директория для загруженных файлов на диске
# UPLOAD_DIR=uploads
# URL-префикс для доступа к файлам (по умолчанию: /uploads)
# UPLOAD_BASE_URL=/uploads

# ─── Session (опционально) ────────────────────────
# Объявлен в конфиге; используется при необходимости сессий
# SESSION_SECRET=your-session-secret-here
