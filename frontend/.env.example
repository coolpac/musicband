# Example env for frontend (Vite)
#
# Copy to `.env.local` (or `.env.development.local`) for local development.
# Only variables prefixed with `VITE_` are exposed to the client.

VITE_USE_MOCK=true
VITE_API_URL=

